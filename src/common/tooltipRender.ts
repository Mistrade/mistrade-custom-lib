import {TooltipProps} from "../components/Tooltip/Tooltip";

interface GetTooltipPositionOptions {
	placement: TooltipProps['placement'],
	containerCoords: DOMRect,
	tooltipHeight: number,
}

export const getTooltipPosition = () => {

}